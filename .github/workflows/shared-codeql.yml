name: Shared CodeQL analysis

on:
  workflow_call:
    inputs:
      source-path:
        description: Path to analyze (defaults to repository root)
        type: string
        default: "."

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze JavaScript
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: [javascript]
    steps:
      - name: Checkout code
        uses: actions/checkout@v7.0.1

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4.37.7
        with:
          languages: ${{ matrix.language }}
          queries: +security-extended,security-and-quality
          source-root: ${{ inputs.source-path }}
          # Offline tooling that downloads curated assets into the repo / .cache.
          # Not shipped sim runtime — see doc/codeql.md.
          config: |
            paths-ignore:
              - '**/scripts/decompile-flash.ts'
              - '**/scripts/data/**'

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4.37.7

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v4.37.7
        with:
          category: "/language:${{ matrix.language }}"
